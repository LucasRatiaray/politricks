<div class="alert alert-info">
    <div class="flex items-center gap-2">
        <span class="text-info">{{ icon|default('<svg width=\"18\" height=\"18\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" viewBox=\"0 0 24 24\"><rect x=\"3\" y=\"3\" width=\"18\" height=\"18\" rx=\"2\"/></svg>')|raw }}</span>
        <div>
            <div class="text-sm font-medium">{{ text }}</div>
            <div class="text-xs opacity-70">{{ date }}</div>
        </div>
    </div>
</div> 