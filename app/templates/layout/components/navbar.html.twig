<nav class="bg-slate-900 px-4 sm:px-6 py-3 flex items-center justify-between border-b border-slate-800">
    <div class="flex items-center gap-4">
        
        <button id="burger-menu-btn" class="md:hidden text-gray-200 hover:text-white">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
        
        <div class="hidden md:flex items-center gap-4">
        {% include 'layout/components/nav_link.html.twig' with {
            'href': '/offenses',
            'text': 'Délits'
        } %}
        {% include 'layout/components/nav_link.html.twig' with {
            'href': '/politics',
            'text': 'Politiciens'
        } %}
        {% include 'layout/components/nav_link.html.twig' with {
            'href': '/partis',
            'text': 'Partis'
        } %}
        {% include 'layout/components/nav_link.html.twig' with {
            'href': '/partners',
            'text': 'Partenaires'
        } %}
        {% include 'layout/components/nav_link.html.twig' with {
            'href': '/medias',
            'text': 'Médias'
        } %}
        </div>
    </div>
    
    <div class="hidden md:flex items-center gap-4">
        {% if app.user %}
            {% include 'layout/components/nav_link.html.twig' with {
                'href': '/profile',
                'text': app.user.firstName,
                'isUser': true
            } %}
            {% include 'layout/components/nav_link.html.twig' with {
                'href': '/logout',
                'text': 'Déconnexion'
            } %}
        {% else %}
            {% include 'layout/components/nav_link.html.twig' with {
                'href': '/login',
                'text': 'Login'
            } %}
            {% include 'layout/components/nav_link.html.twig' with {
                'href': '/register',
                'text': 'Register'
            } %}
        {% endif %}
    </div>
    
    <div class="md:hidden">
        {% if app.user %}
            <a href="/profile" class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center text-white text-sm font-semibold">
                {{ app.user.firstName|first|upper }}
            </a>
        {% endif %}
    </div>
</nav>

<div id="menu-overlay" class="fixed inset-0 bg-black bg-opacity-50 z-40 hidden md:hidden"></div>
<div id="side-menu" class="fixed top-0 left-0 h-full w-64 bg-slate-900 transform -translate-x-full transition-transform duration-300 ease-in-out z-50 md:hidden">
    <div class="flex items-center justify-between p-4 border-b border-slate-800">
        <span class="text-white text-lg font-semibold">PoliTricks</span>
        <button id="close-menu-btn" class="text-gray-400 hover:text-white">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
        </button>
    </div>
    <nav class="p-4">
        <div class="space-y-2">
            <a href="/offenses" class="block text-gray-200 hover:bg-slate-800 rounded px-3 py-2 transition-colors">Délits</a>
            <a href="/politics" class="block text-gray-200 hover:bg-slate-800 rounded px-3 py-2 transition-colors">Politiciens</a>
            <a href="/partners" class="block text-gray-200 hover:bg-slate-800 rounded px-3 py-2 transition-colors">Partenaires</a>
            <a href="/medias" class="block text-gray-200 hover:bg-slate-800 rounded px-3 py-2 transition-colors">Médias</a>
        </div>
        {% if app.user %}
            <div class="border-t border-slate-800 mt-4 pt-4 space-y-2">
                <a href="/profile" class="block text-gray-200 hover:bg-slate-800 rounded px-3 py-2 transition-colors">Mon profil</a>
                <a href="/logout" class="block text-gray-200 hover:bg-slate-800 rounded px-3 py-2 transition-colors">Déconnexion</a>
            </div>
        {% endif %}
    </nav>
</div>