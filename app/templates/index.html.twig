{% extends "layout/dashboard.html.twig" %}

{% block content %}
    <div class="min-h-screen bg-base-200 p-8">
        <div class="max-w-6xl mx-auto">
            <!-- Hero Section -->
            <div class="hero bg-base-100 rounded-box shadow-xl mb-8">
                <div class="hero-content text-center">
                    <div class="max-w-md">
                        <h1 class="text-5xl font-bold">PoliTricks</h1>
                        <p class="py-6">Suivez les activités politiques et les délits en temps réel</p>
                    </div>
                </div>
            </div>

            <!-- Stats Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                {% include 'layout/components/card.html.twig' with { 
                    title: 'Délits', 
                    value: totalDelits|number_format(0, ',', ' '),
                } %}
                {% include 'layout/components/card.html.twig' with { 
                    title: 'Politiciens', 
                    value: totalPoliticiens|number_format(0, ',', ' '),
                } %}
                {% include 'layout/components/card.html.twig' with { 
                    title: 'Partenaires', 
                    value: totalPartenaires|number_format(0, ',', ' '),
                } %}
            </div>
            
            <!-- Recent Activities -->
            <div class="card bg-base-100 shadow-xl">
                <div class="card-body">
                    <h2 class="card-title text-2xl mb-4">Activités récentes</h2>
                    <div class="space-y-4">
                        {% if recentActivities is empty %}
                            <div class="text-center py-8">
                                <div class="text-6xl mb-4">📊</div>
                                <p class="text-base-content/70">Aucune activité récente</p>
                            </div>
                        {% else %}
                            {% for activity in recentActivities %}
                                {% include 'layout/components/activity_item.html.twig' with {
                                    icon: activity.icon,
                                    text: activity.text,
                                    date: activity.date
                                } %}
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}